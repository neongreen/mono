name: claude-trace

on:
  push:
    paths:
      - 'claude-trace/**'
      - '.github/workflows/claude-trace.yml'
  pull_request:
    paths:
      - 'claude-trace/**'
      - '.github/workflows/claude-trace.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24.7'
      
      - name: Run tests
        working-directory: claude-trace
        run: go test ./... -v
