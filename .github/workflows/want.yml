name: want

on:
  push:
    paths:
      - 'want/**'
      - '.github/workflows/want.yml'
  pull_request:
    paths:
      - 'want/**'
      - '.github/workflows/want.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24.7'
          cache-dependency-path: want/go.sum
      
      - name: Run tests
        working-directory: want
        run: go test ./... -v
