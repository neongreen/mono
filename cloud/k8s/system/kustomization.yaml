apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Order matters: gvisor first, then ingress, then cert-manager
  - gvisor/runtimeclass.yaml
  - ingress-nginx
  - cert-manager
