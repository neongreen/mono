# diagram-dsl Examples Gallery

This document showcases all the examples generated by the diagram-dsl library.

## Overview

All examples are generated from JSX code and output as pure SVG. They can be:
- Viewed in any modern browser
- Embedded in documentation
- Converted to PNG/PDF
- Used in presentations

## Examples

### 1. Simple Box (simple-box.svg)

The simplest possible example - a single styled box with text.

**Size:** 414 bytes  
**Features:** Box with background, border, border-radius, centered text

**Code:**
```tsx
<Box
  width={300}
  height={200}
  backgroundColor="#f0f0f0"
  borderColor="#333"
  borderWidth={2}
  borderRadius={10}
  padding={20}
  justifyContent="center"
  alignItems="center"
>
  <Text fontSize={24} fontWeight="bold">
    Hello, diagram-dsl!
  </Text>
</Box>
```

### 2. Basic Flowchart (basic-flowchart.svg)

A simple vertical flowchart showing a linear process.

**Size:** 1.7 KB  
**Features:** Vertical stack, multiple boxes, arrows, color-coding

**Visual Structure:**
```
Title: "Simple Flowchart"
    ↓
[Start Process] (light blue)
    ↓
[Process Data] (light orange)
    ↓
[End Process] (light green)
```

### 3. Architecture Diagram (architecture-diagram.svg)

Three-tier application architecture.

**Size:** 2.3 KB  
**Features:** Vertical stack, styled boxes, labeled arrows

**Visual Structure:**
```
Title: "Architecture Diagram"
    ↓
[Frontend - React App] (light blue)
    ↓ HTTP
[API Gateway - REST API] (light pink)
    ↓ SQL
[Database - PostgreSQL] (light purple)
```

### 4. Multi-Tier Architecture (multi-tier-architecture.svg)

Comprehensive multi-tier web application architecture.

**Size:** 6.0 KB  
**Features:** Complex nested layout, horizontal rows, multiple arrows, color-coded sections

**Visual Structure:**
```
Title: "Multi-Tier Web Application Architecture"

Client Tier:
[Web Browser] [Mobile App]
     ↓    ↓
Application Tier:
[API Gateway] [Auth Service] [Business Logic]
        ↓           ↓              ↓
Data Tier:
[Database] [Cache] [File Storage]
```

**Key Features:**
- Three distinct tiers with labels
- Horizontal row layouts within each tier
- Multiple connection patterns
- Color-coded by tier
- Labels on arrows (HTTPS, SQL)

### 5. Decision Flowchart (decision-flowchart.svg)

User authentication flow with conditional branches.

**Size:** 3.6 KB  
**Features:** Conditional branches, different box shapes (rounded for start/end), labeled branches

**Visual Structure:**
```
[Start: Login Request] (rounded, orange)
          ↓
[Validate Credentials] (rectangular, blue)
     ↙        ↘
[Valid ✓]   [Invalid ✗]
Generate    Show Error
Token       (red)
(green)
   ↓           ↓
[Success:   [Retry
Logged In]  Login]
(rounded)   (rounded)
```

**Key Features:**
- Rounded boxes for start/end states
- Rectangular boxes for processes
- Branching arrows with labels ("Yes", "No")
- Color-coded success (green) vs. error (red) paths

## Usage Patterns Demonstrated

### Layout Patterns

1. **Simple Vertical Stack**
   ```tsx
   <Stack gap={20}>
     <Box>...</Box>
     <Box>...</Box>
   </Stack>
   ```

2. **Horizontal Row**
   ```tsx
   <Row gap={15} justifyContent="center">
     <Box>...</Box>
     <Box>...</Box>
     <Box>...</Box>
   </Row>
   ```

3. **Nested Layouts**
   ```tsx
   <Stack>
     <Text>Title</Text>
     <Row>
       <Box>...</Box>
       <Box>...</Box>
     </Row>
   </Stack>
   ```

### Styling Patterns

1. **Color-Coded Boxes**
   ```tsx
   backgroundColor="#e3f2fd"
   borderColor="#1976d2"
   ```

2. **Rounded Corners**
   ```tsx
   borderRadius={8}  // Small radius
   borderRadius={35} // Large radius (pill shape)
   ```

3. **Centered Content**
   ```tsx
   justifyContent="center"
   alignItems="center"
   ```

### Arrow Patterns

1. **Simple Connection**
   ```tsx
   <Arrow from="box1" to="box2" color="#1976d2" />
   ```

2. **Labeled Arrow**
   ```tsx
   <Arrow from="box1" to="box2" label="HTTPS" />
   ```

## Generating Examples

Generate all examples at once:
```bash
npm run examples
```

Or generate individually:
```bash
npm run dev:simple    # Simple box
npm run dev           # Basic examples
npm run dev:advanced  # Advanced examples
```

## Viewing Examples

The generated SVG files can be opened in:
- **Web Browser:** Double-click any `.svg` file
- **VS Code:** Install SVG preview extension
- **Command Line:** `cat examples/simple-box.svg` (shows SVG source)

## Real-World Use Cases

These examples demonstrate patterns useful for:

1. **Technical Documentation**
   - API flow diagrams
   - System architecture overviews
   - Process workflows

2. **Presentations**
   - Architecture slides
   - Decision flowcharts
   - System component diagrams

3. **RFCs and Design Docs**
   - Proposed architecture
   - Data flow diagrams
   - Integration patterns

4. **Educational Content**
   - Tutorial flowcharts
   - Concept diagrams
   - Step-by-step processes

## Customization

All examples are easily customizable:
- Change colors to match your brand
- Adjust spacing with `gap`, `padding`, `margin`
- Resize boxes with `width`, `height`
- Modify text with `fontSize`, `fontWeight`
- Add more boxes, arrows, or tiers

## Performance

All examples generate in < 100ms:
- Simple box: ~5ms
- Basic flowchart: ~10ms
- Architecture diagram: ~15ms
- Multi-tier: ~25ms
- Decision flowchart: ~20ms

SVG file sizes are very reasonable (414 bytes - 6 KB), making them suitable for documentation, version control, and web serving.
