import React from 'react';
import { ContextWindowProps } from '../types';

export const ContextWindow: React.FC<ContextWindowProps> = (props) => {
  return React.createElement('ContextWindow', props, null);
};
