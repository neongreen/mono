import React from 'react';
import { LoopIndicatorProps } from '../types';

export const LoopIndicator: React.FC<LoopIndicatorProps> = (props) => {
  return React.createElement('LoopIndicator', props, null);
};
