import React from 'react';
import { MemoryBlockProps } from '../types';

export const MemoryBlock: React.FC<MemoryBlockProps> = (props) => {
  return React.createElement('MemoryBlock', props, null);
};
