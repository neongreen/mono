import React from 'react';
import { ProcessNodeProps } from '../types';

export const ProcessNode: React.FC<ProcessNodeProps> = (props) => {
  return React.createElement('ProcessNode', props, null);
};
